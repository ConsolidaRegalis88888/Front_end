import {useSelector} from "react-redux";
import React from 'react';

const AllPosts = () => {
    //const allPosts = useSelector<Store, SinglePost>(state => state.blog.data);

    return <>
        <div >
            {}
        </div>
    </>
};

export default AllPosts;
