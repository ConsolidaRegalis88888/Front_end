export default class Subscription
{
    text
    mainTitle   //String
    image //file

    constructor(text, mainTitle, image) {
        this.text = text;
        this.mainTitle = mainTitle;
        this.image = image;
    }
}