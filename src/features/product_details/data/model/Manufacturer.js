export default class Manufacturer {
    id
    name
    logo
    comment

    constructor(id, name, logo, comment) {
        this.id = id;
        this.name = name;
        this.logo = logo;
        this.comment = comment;
    }
}